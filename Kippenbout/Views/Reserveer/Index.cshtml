@model IEnumerable<Kippenbout.Models.ReserveerModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Menu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tijd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Menu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personen)
        </td>
        <td>
          @Html.DisplayFor(modelItem => item.Tijd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RevereringID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RevereringID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RevereringID })
        </td>
    </tr>
}

</table>
